specVersion: "v1"
serviceId: "swustl"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "swustl-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
